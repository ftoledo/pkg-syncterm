#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_install:
	$(MAKE) -C src/syncterm USE_SDL=1 PREFIX=$$(pwd)/debian/syncterm/usr install

